package com.calendly.clone.entity.enums;

// ─── UserRole.java ───────────────────────────────────────────────────────────

public enum UserRole {
    ROLE_USER,   // regular host account
    ROLE_ADMIN   // platform-level admin (Calendly staff, not team admin)
}